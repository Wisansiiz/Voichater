port: 9000
release: false

mysql:
  user: ${MYSQL_USER}
  password: ${MYSQL_PWD}
  host: mysql.sqlpub.com
  port: 3306
  db: online_voice_channel

redis:
  redisURL: ${REDIS_URL}
  redisDbName: 0
  redisHost: ${REDIS_HOST}
  redisPort: ${REDIS_PORT}
  redisUsername:
  redisPassword: ${REDIS_PSW}
  redisNetwork: "tcp"

cache:
  cacheType: redis
  cacheEmpires: 600
  cacheWarmUp:
  cacheServer:

encryptSecret:
  jwtSecret: "FanOne666Secret"
  emailSecret: "EmailSecret"
  phoneSecret: "PhoneSecret"

oss:
  AccessKeyId:
  AccessKeySecret:
  BucketName:
  QiNiuServer:

rabbitMq:
  rabbitMQ: amqp
  rabbitMQUser: guest
  rabbitMQPassWord: guest
  rabbitMQHost: localhost
  rabbitMQPort: 5672